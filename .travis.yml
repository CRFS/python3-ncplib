language: python
sudo: false
python:
- 3.4
install:
  - travis_retry pip install -e . coverage
cache:
  directories:
    - $HOME/.cache/pip
script:
  - coverage run setup.py test
after_script:
  - coverage report
notifications:
  on_success: never
  on_failure: change
