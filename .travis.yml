language: python
sudo: false
python:
- 3.4
install:
  - travis_retry pip install -e .[test]
cache:
  directories:
    - $HOME/.cache/pip
script:
  - py.test --cov ncplib tests
notifications:
  on_success: never
  on_failure: change
